server:
  port: 0
spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: warehouse
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
